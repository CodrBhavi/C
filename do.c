// Do-while loops
#include <stdio.h>
#include <conio.h>
int main()
{
    /* code */
    int no = 1;
    do
    {
        printf("\t %d", no);
        no = no + 1;
    } while (no <= 100);
    return 0;
}
