#include <stdio.h>
#include <conio.h>
int main(int argc, char const *argv[])
{
    /* code */
    int no = 1;
    for (no = 1; no <= 101; no = no + 2)
    {
        printf("\t%d", no);
    }
    return 0;
}
